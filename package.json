{
  "name": "growmark",
  "version": "1.0.0",
  "description": "Growmark — AI Watermark Remover by Growistan Venture",
  "private": true,
  "scripts": {
    "build": "node build.js --production",
    "build:dev": "node build.js",
    "serve": "node scripts/serve.js",
    "start": "node build.js --production && node scripts/serve.js",
    "install-esbuild": "node scripts/install-esbuild.js",
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --import ./tests/register-loader.mjs tests/engine-regression.mjs"
  },
  "dependencies": {
    "exifr": "^7.1.3",
    "jszip": "^3.10.1",
    "medium-zoom": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
